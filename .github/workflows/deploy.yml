name: Deploy to GitHub Pagesname: Deploy to GitHub Pages



on:on:

  push:  push:

    branches:    branches: [ "master" ]

      - master  workflow_dispatch:



permissions:permissions:

  contents: write  contents: write

  pages: write

jobs:  id-token: write

  build-and-deploy:

    runs-on: ubuntu-latestjobs:

    steps:  build-and-deploy:

      - name: Checkout 🛎️    runs-on: ubuntu-latest

        uses: actions/checkout@v3    steps:

      - name: Checkout 🛎️

      - name: Setup Node.js 📦        uses: actions/checkout@v3

        uses: actions/setup-node@v3

        with:      - name: Setup Node.js 📦

          node-version: '18'        uses: actions/setup-node@v3

        with:

      - name: Install Dependencies 🔧          node-version: '18'

        run: npm install          cache: 'npm'



      - name: Build 🏗️      - name: Install dependencies 🔧

        run: npm run build        run: npm install



      - name: Deploy to GitHub Pages 🚀      - name: Build 🏗️

        uses: peaceiris/actions-gh-pages@v3        run: npm run build

        with:

          github_token: ${{ secrets.GITHUB_TOKEN }}      - name: Deploy to GitHub Pages 🚀

          publish_dir: ./dist        uses: JamesIves/github-pages-deploy-action@v4

          force_orphan: true        with:
          folder: dist
          branch: gh-pages
          clean: true
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v3
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2